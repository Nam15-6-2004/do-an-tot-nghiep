create database web ;
use web;

insert into vaitro(maVT, tenVT, createdAt, updatedAt)
values('A00', 'admin', now(), now()),
		('U11', 'user', now(), now());
        
        select * from vaitro;
        select * from nguoidung;
        select * from hoadonban;
        


